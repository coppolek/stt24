services:
  stt24-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stt24-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:80"
    environment:
      - NODE_ENV=production
